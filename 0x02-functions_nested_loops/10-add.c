#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @c: inputs integer 1
 * @d: inputs integer 2
 *
 * Return: c + d result
 */
int add(int c, int d)

{
		return (c + d);
}
